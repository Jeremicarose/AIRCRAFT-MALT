[package]
name = "receiver-registry"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"] }
serde-json-core = "0.5"

[dev-dependencies]
serde_json = "1"
